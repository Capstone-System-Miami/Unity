using UnityEngine;

namespace SystemMiami.Enums
{
    // An enum of directions
    // Starting at the top center,
    // And moving clockwise.
    public enum TileDir
    {
        FORWARD_C,
        FORWARD_R,
        MIDDLE_R,
        BACKWARD_R,
        BACKWARD_C,
        BACKWARD_L,
        MIDDLE_L,
        FORWARD_L,
    }
}
